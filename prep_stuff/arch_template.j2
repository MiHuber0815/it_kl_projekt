@prefix arch: <https://example.org/arch/> .
@prefix dct: <http://purl.org/dc/terms/> .
@prefix skos: <http://www.w3.org/2004/02/skos/core#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .

arch: a skos:ConceptScheme ;
    dct:title "Vokabular Propylaeum-VITAE"@de;
    dct:creator "Annette Frey"@de,"Michael Huber"@de,"Birgit Lechner"@de;
    skos:note "inhaltliche Entwicklung Projektteam Proplyaeum-VITAE";
    skos:note "Propylaeum-VITAE (https://www.propylaeum.de/themen/propylaeum-vitae) ist ein biographisches Informationssystem zu Personen, die durch ihre Leistungen in der Archäologie und in den Altertumswissenschaften hervorgetreten sind. Bei der Erfassung der Informationen zu den erfassten Personen werden nach Möglichkeit hinterlegte kontrollierte Vokabular anstelle von Freitexten verwendet. Dies soll Abfragen strukturieren und Filteranzeigen erleichtern. Die Vokabulare wurden vom Projektteam Propylaeum-VITAE im Rahmen des ‚DFG-Projektes Spuren archäologischer Wissensgenerierung. Propylaeum-VITAE – ein Instrument für die Wissenschaftsgeschichte der Archäologie‘ (2020-2024) entwickelt." ; 
Die verwendeten Vokabulare sollen sukzessive mit SKOS publiziert werden. Die hier vorgelegte, bislang umfangreichste Liste enthält Berufsrollen von Personen, in einer Zeit, in der die Archäologie noch nicht professionalisiert war. 
    dct:created "2025-12-22"^^xsd:date ;
    dct:license <https://creativecommons.org/publicdomain/zero/1.0/> ;
    skos:hasTopConcept arch:forschungstätigkeit, arch:studienfach, arch:berufsrolle .

arch:forschungstätigkeit a skos:Concept ;
    skos:prefLabel "Forschungstätigkeit"@de;
    skos:topConceptOf arch: .

arch:studienfach a skos:Concept ;
    skos:prefLabel "Studienfach"@de ;
    skos:narrower arch:afrikanischeArchäologie;
    skos:narrower arch:ägyptischePhilologie;
    skos:topConceptOf arch: .
    
arch:afrikanischeArchäologie a skos:Concept ;
    skos:prefLabel "Afrikanische Archäologie"@de ;
    skos:inScheme arch: .

arch:ägyptischePhilologie a skos:Concept ;
    skos:prefLabel "Ägyptische Philologie"@de ;
    skos:inScheme arch: .



arch:berufsrolle a skos:Concept ;
    skos:prefLabel "Berufsrolle"@de;
    {% for concept in concepts -%}
        {% if concept not in narrower_concepts -%}
    skos:narrower arch:{{ concept}} ;
        {% endif -%}
    {% endfor -%}
    skos:topConceptOf arch: .


{% for concept in concepts -%}
arch:{{ concept}} a skos:Concept ;
{% for verb, value in concepts[concept].items() -%}
    {% if value is  not none -%}
    {% if value is iterable -%}
        {% if value is string -%} 
            {% if  verb in ["skos:narrower", "skos:broader"] -%}
            {{verb}} arch:{{ value }} ;
        {% else -%}
            {{verb}} "{{ value }}"{% if verb in statics %}@de{% endif %} ;
        {% endif -%}
        {% endif -%}
    {% if value is not string and value is not mapping -%}
    {% for val in value -%}
        {% if  verb in ["skos:narrower", "skos:broader"] -%}
            {{verb}} arch:{{ val }} ;
        {% else -%}
            {{verb}} "{{ val }}"{% if verb in statics %}@de{% endif %} ;
        {% endif -%}
    {% endfor -%}
    {% endif -%}
    {% endif -%}
    {% endif -%}
{% endfor -%}
    skos:inScheme arch: .
{% endfor -%}


